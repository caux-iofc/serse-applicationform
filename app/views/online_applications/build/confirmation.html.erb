
<%= render :layout => 'form_application_group' do |f| %>

<div class="col-md-12 text-center">
    <h3><%=t :name_badge %></h3>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <%=t :badge_explanation %>
  </div>
</div>

<%= f.fields_for :online_applications do |builder| %>
  <%= render 'group_confirmation', :f => builder, :online_application => builder.object %>
<% end %>

<% end %>
