
  <div class="col-md-12 text-center">
    <h3><%=t :detailed_information %></h3>
  </div>
</div>

<%= f.fields_for :online_applications do |builder| %>
  <%= render 'group_detail', :f => builder, :online_application => builder.object %>
<% end %>

<div class="row">
